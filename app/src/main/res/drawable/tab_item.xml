<?xml version="1.0" encoding="utf-8"?>
<!-- Which pane you are on is stated by fill, not by a faint underline. During a demo
     the audience is three metres away and reading a two-pixel line is not on offer. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/teal_dark" />
            <corners android:radius="9dp" />
            <stroke android:width="1dp" android:color="@color/teal_deep" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="9dp" />
        </shape>
    </item>
</selector>
